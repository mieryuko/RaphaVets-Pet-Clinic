[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[start]
# Use gunicorn with uvicorn workers for production. Adjust workers based on available RAM.
cmd = "gunicorn -k uvicorn.workers.UvicornWorker app:app --bind 0.0.0.0:$PORT --workers 1"
